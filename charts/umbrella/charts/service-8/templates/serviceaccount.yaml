{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "service-8.serviceAccountName" . }}
  labels:
{{- include "service-8.labels" . | nindent 4 }}
{{- end }}
