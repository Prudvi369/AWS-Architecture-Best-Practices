{{- if .Values.config.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "service-8.fullname" . }}
  labels:
{{- include "service-8.labels" . | nindent 4 }}
data:
{{- toYaml .Values.config.data | nindent 2 }}
{{- end }}
